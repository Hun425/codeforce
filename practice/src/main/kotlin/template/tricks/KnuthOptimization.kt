package template.tricks

/**
 * ✅ Knuth 최적화 스켈레톤
 * - 구간 DP에서 opt[i][j-1] <= opt[i][j] <= opt[i+1][j] + 사거리 조건이 성립할 때 O(N^2)로 감소
 */
object KnuthOptimizationSkeleton {
    // 예시: 최적 이진 검색 트리, 행렬 체인 곱 등
}
